#include<stdio.h>
int main()
{
    int i,j,k;
    printf("ernter theare no if you find how is big \n");
    scanf("%d%d%d",&i,&j,&k);
    if(i>j)
    {
        if(i>k);
        printf("you first no is %d largest no rader than that\n",i);
    }
    else if(j>k)
    {
        if(j>i);
        printf("your 2nd no %d is largest \n",j);
    }
    else
    printf("your last value %d is largesr\n",k);
return 0;
}